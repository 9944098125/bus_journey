version: '3.8'

services:
  frontend:
    build: 
      context: ./apps/frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"

  admin:
    build: 
      context: ./apps/admin_panel
      dockerfile: Dockerfile
    ports:
      - "3001:3000"

  backend:
    build: 
      context: ./apps/backend
      dockerfile: Dockerfile
    ports:
      - "5001:3000"
