# CRA/webpack plugins (react-scripts, webpack-manifest-plugin, fork-ts-checker)
# require a single webpack instance. pnpm's default isolated linker splits webpack
# into per-peer-context copies, which breaks `compilation instanceof Compilation`.
# A fully-hoisted (flat) node_modules guarantees one instance of each package.
node-linker=hoisted
shamefully-hoist=true
