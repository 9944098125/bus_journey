import { describe, it, expect } from 'vitest';
import * as admin_dashboard_controller from '../admin-dashboard.controller';

describe('admin-dashboard controller', () => {
  it('should be defined', () => {
    expect(admin_dashboard_controller).toBeDefined();
  });
});
