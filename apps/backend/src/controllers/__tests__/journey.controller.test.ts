import { describe, it, expect } from 'vitest';
import * as journey_controller from '../journey.controller';

describe('journey controller', () => {
  it('should be defined', () => {
    expect(journey_controller).toBeDefined();
  });
});
