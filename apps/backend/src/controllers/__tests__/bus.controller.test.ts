import { describe, it, expect } from 'vitest';
import * as bus_controller from '../bus.controller';

describe('bus controller', () => {
  it('should be defined', () => {
    expect(bus_controller).toBeDefined();
  });
});
