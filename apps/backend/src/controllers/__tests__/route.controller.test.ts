import { describe, it, expect } from 'vitest';
import * as route_controller from '../route.controller';

describe('route controller', () => {
  it('should be defined', () => {
    expect(route_controller).toBeDefined();
  });
});
