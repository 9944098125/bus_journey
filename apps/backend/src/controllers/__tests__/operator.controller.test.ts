import { describe, it, expect } from 'vitest';
import * as operator_controller from '../operator.controller';

describe('operator controller', () => {
  it('should be defined', () => {
    expect(operator_controller).toBeDefined();
  });
});
