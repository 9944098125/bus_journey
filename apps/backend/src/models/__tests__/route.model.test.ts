import { describe, it, expect } from 'vitest';
import * as route_model from '../route.model';

describe('route model', () => {
  it('should be defined', () => {
    expect(route_model).toBeDefined();
  });
});
