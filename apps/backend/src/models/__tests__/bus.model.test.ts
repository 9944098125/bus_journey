import { describe, it, expect } from 'vitest';
import * as bus_model from '../bus.model';

describe('bus model', () => {
  it('should be defined', () => {
    expect(bus_model).toBeDefined();
  });
});
