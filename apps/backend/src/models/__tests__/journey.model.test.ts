import { describe, it, expect } from 'vitest';
import * as journey_model from '../journey.model';

describe('journey model', () => {
  it('should be defined', () => {
    expect(journey_model).toBeDefined();
  });
});
