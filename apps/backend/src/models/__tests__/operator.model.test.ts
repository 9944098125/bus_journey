import { describe, it, expect } from 'vitest';
import * as operator_model from '../operator.model';

describe('operator model', () => {
  it('should be defined', () => {
    expect(operator_model).toBeDefined();
  });
});
