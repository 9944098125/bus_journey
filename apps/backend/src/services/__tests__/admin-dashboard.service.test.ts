import { describe, it, expect } from 'vitest';
import * as admin_dashboard_service from '../admin-dashboard.service';

describe('admin-dashboard service', () => {
  it('should be defined', () => {
    expect(admin_dashboard_service).toBeDefined();
  });
});
