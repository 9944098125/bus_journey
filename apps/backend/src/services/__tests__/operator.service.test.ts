import { describe, it, expect } from 'vitest';
import * as operator_service from '../operator.service';

describe('operator service', () => {
  it('should be defined', () => {
    expect(operator_service).toBeDefined();
  });
});
