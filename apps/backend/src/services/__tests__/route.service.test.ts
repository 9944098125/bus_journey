import { describe, it, expect } from 'vitest';
import * as route_service from '../route.service';

describe('route service', () => {
  it('should be defined', () => {
    expect(route_service).toBeDefined();
  });
});
