import { describe, it, expect } from 'vitest';
import * as route_route from '../route.route';

describe('route route', () => {
  it('should be defined', () => {
    expect(route_route).toBeDefined();
  });
});
