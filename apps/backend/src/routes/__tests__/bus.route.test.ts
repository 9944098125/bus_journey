import { describe, it, expect } from 'vitest';
import * as bus_route from '../bus.route';

describe('bus route', () => {
  it('should be defined', () => {
    expect(bus_route).toBeDefined();
  });
});
