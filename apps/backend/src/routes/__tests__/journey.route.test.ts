import { describe, it, expect } from 'vitest';
import * as journey_route from '../journey.route';

describe('journey route', () => {
  it('should be defined', () => {
    expect(journey_route).toBeDefined();
  });
});
