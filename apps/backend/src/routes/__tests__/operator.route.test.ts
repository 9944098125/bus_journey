import { describe, it, expect } from 'vitest';
import * as operator_route from '../operator.route';

describe('operator route', () => {
  it('should be defined', () => {
    expect(operator_route).toBeDefined();
  });
});
