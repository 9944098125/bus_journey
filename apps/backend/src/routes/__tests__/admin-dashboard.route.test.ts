import { describe, it, expect } from 'vitest';
import * as admin_dashboard_route from '../admin-dashboard.route';

describe('admin-dashboard route', () => {
  it('should be defined', () => {
    expect(admin_dashboard_route).toBeDefined();
  });
});
