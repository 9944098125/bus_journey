/** Re-export dashboard API types (shared with RTK Query in Dashboard/slice). */
export type {
  AuthProviderBreakdownItem as AuthProviderBreakdown,
  DashboardKpis,
  DashboardUserSnapshot,
  FullDashboardData,
  RoleBreakdownItem as RoleBreakdown,
} from 'types/dashboard';
